import { FC } from 'react';
import { View } from '@tarojs/components';
import styles from './index.module.less';

interface IProps {}

const List: FC<IProps> = () => <View className={styles.container}>List</View>;

export default List;
